import React from 'react';

const UserInfo = (info) =>{
  
  const {pic,name,start_rating,years,mentees,mentors} = info

  return(
    <div>
      this is the user info for dashboard
    </div>
  )
}

export default UserInfo;